name = "horrorwriter"
main = "./dist/server/entry.mjs"
compatibility_date = "2024-11-01"
compatibility_flags = [ "nodejs_compat" ]

[assets]
directory = "./dist/client"
binding = "ASSETS"

# Build/runtime env vars. Workers Builds reads this file as the source of truth.
# VITE_* vars are baked into the bundle at build time by Astro — they must live here
# so Workers Builds injects them during `npm run build`. All values below are public
# by design (Supabase *publishable* key is RLS-protected) — safe to commit.
# See CLAUDE.md → Infrastructure.
[vars]
VITE_SUPABASE_URL = "https://bmvvugrfnuedjlucmlbw.supabase.co"
VITE_SUPABASE_ANON_KEY = "sb_publishable_-Y5K0e8qQrpsybmuQyyqxg_llQOokRY"
VITE_TRANSCRIBE_URL = "https://horrorwriter-transcribe.orig-beetlebub.workers.dev"
VITE_TURNSTILE_SITE_KEY = "0x4AAAAAADVOj70TqBIdnJER"
VITE_ENABLE_GOOGLE_LOGIN = "true"
