name = "horrorwriter"
compatibility_date = "2024-01-01"
pages_build_output_dir = "./dist"

# Build/runtime env vars. Cloudflare Pages treats this wrangler config as the source
# of truth and WIPES dashboard plain-text vars on every deploy (secrets survive). So
# every VITE_* var the build needs MUST live here, not in the dashboard. All values
# below are public by design (Supabase *publishable* key is RLS-protected; Turnstile
# *site* key is client-side) — safe to commit. The Turnstile SECRET key stays in
# Supabase Auth, never here. See CLAUDE.md → Infrastructure.
[vars]
VITE_SUPABASE_URL = "https://bmvvugrfnuedjlucmlbw.supabase.co"
VITE_SUPABASE_ANON_KEY = "sb_publishable_-Y5K0e8qQrpsybmuQyyqxg_llQOokRY"
VITE_TRANSCRIBE_URL = "https://horrorwriter-transcribe.orig-beetlebub.workers.dev"
VITE_TURNSTILE_SITE_KEY = "0x4AAAAAADVOj70TqBIdnJER"
VITE_ENABLE_GOOGLE_LOGIN = "true"
