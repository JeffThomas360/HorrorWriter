name = "horrorwriter-transcribe"
main = "src/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]   # for `node:buffer` (base64 encoding)
account_id = "e61bdda6d2e023366f97a9bf015c6334"

[ai]
binding = "AI"

[observability]
enabled = true   # persist console logs so transcription failures are debuggable

# Used to verify the caller's Supabase access token (src/auth.js). Both are
# public values; the anon key is the same one the site ships to every browser.
[vars]
SUPABASE_URL = "https://bmvvugrfnuedjlucmlbw.supabase.co"
SUPABASE_ANON_KEY = "sb_publishable_-Y5K0e8qQrpsybmuQyyqxg_llQOokRY"
